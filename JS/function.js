//()=>{}//arrow function
//const sum = (a,b) => {//a function is created to perform specific task for program
    // the (a,b) is called parameter
    //console.log("Hello");
    //const a = 20;
    //const b = 30;
    //console.log(a + b)//string concatenation 
    //string is powerful

//}
//operators are (+,-,*,/) //arithematic operator
// (&&, ||) => //logical operator

//sum(10 , 10); //when we create function we should always call it to execute 
//the value passed inside ()i.e (10, 10) is argument

//function sum (){}

//function divide (){}

const sum = (a,b) => {

    let add = a + b;
    console.log(add)
}

const multiply = (a,b) => {
    let multiply = a * b;
    console.log(multiply)
}
sum(10,20)
multiply(10,10)
